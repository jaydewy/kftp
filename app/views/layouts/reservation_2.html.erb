<!DOCTYPE html>
<html>
  <head>
    <title>Kinmount Fair Trailer Park</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "reservation_print", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="content" >
      <div class="logo-header">
        <%= image_tag "KFTPLogo_02282023.png", alt: "KFTP Logo", height: 50 %>
      </div>
      <%= render :partial => 'layouts/header' %>
      <%= render :partial => 'layouts/sidebar' %>
      <div class="main">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
